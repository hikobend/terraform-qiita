name: echo_hello

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  echo_hello:
    name: 
    runs-on: ubuntu-latest
    steps:
      - name: echo hello
        run: echo "hello"
